import SvgSpinner from "public/svgs/spinner.svg";

const Spinner = () => {
  return (
    <div className="w-full flex justify-center">
      <SvgSpinner />
    </div>
  );
};

export default Spinner;
